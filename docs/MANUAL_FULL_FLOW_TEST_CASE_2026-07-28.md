# 维修系统全流程人工回归测试用例

测试日期：2026-07-28  
测试环境：http://127.0.0.1:3000  
测试目的：从空业务数据库开始，验证五种角色、主流程、收费/免费分支、返厂、取消、聊天、报表和权限。

## 1. 测试前状态

- Repair_Tickets：0
- Repair_Ticket_History：0
- TicketMessage：0
- Customer_History：0
- Batch_Number_Sequence：0
- 保留 Users：6
- 保留 Device_Inventory：32,538
- 保留 Product_Catalog：86
- 下一张批次工单应为 `WO2607280001`。

## 2. 测试账号

| 角色 | 用户名 | 密码 | 主要入口 |
| --- | --- | --- | --- |
| 管理员 | `admin` | `TEST_USER_PASSWORD` | `/admin/users` |
| 维修人员 | `wx` | `TEST_USER_PASSWORD` | `/` |
| 仓库人员 | `ck` | `TEST_USER_PASSWORD` | `/warehouse/dashboard` |
| 现场人员 | `xc` | `TEST_USER_PASSWORD` | `/report` |
| 商务人员 | `sw` | `TEST_USER_PASSWORD` | `/business` |
| 第二现场人员 | `xc2` | `TEST_USER_PASSWORD` | `/report` |

建议给不同角色分别使用不同浏览器配置文件或无痕窗口；如果只用一个窗口，每次切换角色前先退出登录。

准备两张图片：

- 设备故障照片：可使用 `public/machine-fault-photo.jpg`。
- 签字凭证：任意 JPG/PNG 测试图片。

统一产品目录测试值：

- 一级分类：`读卡器`
- 二级分类：`读卡器`
- 型号：`AX-7CW`
- 型号代码：`READER-CARD-AX7CW`

## 3. 主流程 A：收费维修，多设备、多最终结果

### A01 现场创建批次

使用 `xc` 登录，进入 `/report`，创建两台设备的批次工单：

| 字段 | 测试值 |
| --- | --- |
| 项目名称 | `全流程回归-收费-20260728` |
| 项目位置 | `上海测试现场` |
| 联系人 | `张测试` |
| 联系电话 | `13800138000` |
| 设备 A 序列号 | `MANUAL-PAID-20260728-A` |
| 设备 B 序列号 | `MANUAL-PAID-20260728-B` |
| 产品 | `读卡器 / 读卡器 / AX-7CW` |
| 故障描述 | A：无法读卡；B：间歇性离线 |

操作时同时验证：

- [ ] 同一批次内输入重复序列号时，系统阻止提交。
- [ ] 上传设备照片后可以预览。
- [ ] 正常提交成功，记录批次号为 `批次 A`。
- [ ] `批次 A` 预计为 `WO2607280001`。
- [ ] 两台设备共享同一个批次号，状态均为 `Warehouse_Confirming`（待仓库确认）。
- [ ] 现场人员可以编辑尚未被仓库确认的批次信息。

### A02 聊天与身份

在 `批次 A` 详情页发送：`现场：请仓库优先确认本批次`。

- [ ] 消息发送成功，刷新后仍存在。
- [ ] 显示发送人为真实账号“刘工 / Reporter”。

切换到 `ck`，打开同一批次并回复：`仓库：已收到`。

- [ ] 两个角色都能看到完整消息顺序。
- [ ] 仓库消息显示为真实账号“黄工 / Warehouse”。

### A03 仓库确认

使用 `ck` 进入 `/warehouse/dashboard` 的待确认列表，打开 `批次 A`：

- 设备 A 出厂日期：`2026-07-01`。
- 设备 B 出厂日期：`2024-01-01`。

- [ ] 不填写任一设备出厂日期时，确认按钮应被阻止或返回明确提示。
- [ ] 填完后确认成功。
- [ ] 两台设备状态变为 `In_Repair`（维修检查中）。
- [ ] A 显示保内，B 显示过保；如系统判定不同，记录实际保修期配置。
- [ ] 操作日志出现 `WarehouseConfirmed`，操作人为黄工。

### A04 维修报告、返厂字段与回退重发

使用 `wx` 打开 `批次 A`，进入“编辑维修报告”：

| 字段 | 设备 A | 设备 B |
| --- | --- | --- |
| 故障分类 | 硬件损坏 | 其他 |
| 维修动作 | 更换配件 | 返厂维修 |
| 故障点 | 读卡模块损坏 | 主板间歇性离线 |
| 处理说明 | 更换读卡模块并测试 | 送供应商检测 |
| 维修费用 | `120` | `30` |
| 返厂日期 | 不填 | `2026-07-28` |
| 返厂快递单号 | 不填 | `RMA-MANUAL-20260728` |
| 供应商 | 不填 | `测试供应商` |

- [ ] 保存草稿后再次进入，所有字段正确回显。
- [ ] 发送维修报告后，批次状态变为 `Pending_Reporter_Confirm`。
- [ ] 打印/查看报告页面能正常打开并展示两台设备。

在现场签字前，维修人员再次修改设备 A 费用为 `150` 并保存：

- [ ] 状态回退到 `In_Repair`。
- [ ] 操作日志出现 `RepairReportRevised`，并显示 `120 → 150` 的变更。
- [ ] 重新发送后再次进入 `Pending_Reporter_Confirm`。

### A05 现场查看报告和上传签字

使用 `xc` 打开 `批次 A`：

- [ ] 可以查看和打印报告，但不能编辑维修内容。
- [ ] 现场人员看到设备 B 时，不应暴露“返厂维修”、供应商、返厂物流等内部信息。
- [ ] 不选择图片时不能提交签字确认。
- [ ] 上传签字图片并发送成功。
- [ ] 状态变为 `Technician_Repairing`。
- [ ] 操作日志出现 `ReporterConfirmed`，签字图片刷新后仍能查看。

### A06 最终处理结果

使用 `wx` 打开批次，在每台设备详情中选择：

- 设备 A：`维修完成`（`Completed`）。
- 设备 B：`无需维修退回`（`ReturnUnrepaired`）。

- [ ] 未给全部设备选择结果时，“提交全部处理结果”不可用。
- [ ] Reporter 账号不能修改最终处理结果。
- [ ] 两台都选择后提交整批成功。
- [ ] 因总费用为 `180`，状态变为 `Business_Review`。
- [ ] 操作日志出现每台设备的结果和 `TechnicianCompleted`。

### A07 商务审核

使用 `sw` 进入 `/business` 的待审核列表，打开 `批次 A`：

| 字段 | 测试值 |
| --- | --- |
| 是否收费 | 是 |
| 总费用 | `180` |
| 客户名称 | `全流程测试客户` |
| 是否收款 | 是 |
| 是否开票 | 是 |

- [ ] 费用或必填信息缺失时不能完成审核。
- [ ] 完整提交后状态变为 `Warehouse_Shipping`。
- [ ] 操作日志出现 `BusinessReviewed`，操作人为李工。

### A08 仓库发货并结案

使用 `ck` 进入待发货列表，打开 `批次 A`：

- 发货方式：发回客户。
- 快递单号：`SF-MANUAL-PAID-20260728`。
- 发货数量：`2`。

- [ ] 快递单号为空时不能完成发货。
- [ ] 提交后两台设备状态均变为 `Completed`。
- [ ] 操作日志出现 `WarehouseShipped`。
- [ ] 现场、维修、商务、仓库均能查看终态详情和完整时间线。
- [ ] 报表打印或 PDF 下载正常。

## 4. 分支流程 B：免费维修自动跳过商务

使用 `xc` 创建单设备批次：

- 项目：`全流程回归-免费-20260728`
- 序列号：`MANUAL-FREE-20260728-A`
- 产品：`读卡器 / 读卡器 / AX-7CW`

按 A03–A05 完成仓库确认、维修报告和现场签字。维修费用填写 `0`，最终结果选择 `入库处理`（`Scrapped`）。

- [ ] 维修人员提交最终结果后，系统直接进入 `Warehouse_Shipping`。
- [ ] 商务待审核列表中不应出现该批次。
- [ ] 操作日志出现 `BusinessReviewSkipped`。
- [ ] 仓库选择“产品入库”并提交，状态变为 `Completed`。

## 5. 分支流程 C：取消申请批准

使用 `xc` 创建单设备批次：

- 项目：`全流程回归-取消批准-20260728`
- 序列号：`MANUAL-CANCEL-APPROVE-20260728`

在尚未完成时打开批次详情，申请取消，原因填写：`测试取消批准流程`。

- [ ] 原业务状态暂时保持不变，`CancelRequestStatus = Pending`。
- [ ] 重复申请被阻止。
- [ ] 使用 `sw` 在商务页面看到取消提示并点击“批准取消”。
- [ ] 批次状态变为 `Cancelled`，不再出现在待办列表中。
- [ ] 取消日志和操作人正确。

## 6. 分支流程 D：取消申请拒绝

使用 `xc` 创建单设备批次：

- 项目：`全流程回归-取消拒绝-20260728`
- 序列号：`MANUAL-CANCEL-REJECT-20260728`

申请取消，原因填写：`测试取消拒绝流程`；使用 `sw` 选择拒绝。

- [ ] `CancelRequestStatus = Rejected`。
- [ ] 工单没有变成 `Cancelled`，仍停留在申请取消前的业务状态。
- [ ] 后续仓库确认和主流程仍能继续。

## 7. 管理员和权限回归

使用 `admin`：

- [ ] `/admin/users` 能查看 6 个现有账号。
- [ ] 新增临时账号 `manual_user_20260728`，角色 Reporter。
- [ ] 修改临时账号姓名和角色后刷新仍正确。
- [ ] 注销临时账号后不能再登录。
- [ ] 统计页面可以加载真实数字，不出现数据库明细错误。

使用非管理员账号：

- [ ] `xc` 访问用户管理页面应被阻止或跳转。
- [ ] `xc` 直接请求用户管理接口应得到 `403`。
- [ ] 未登录访问用户、统计、商务批次、最终结果、聊天接口应得到 `401`。
- [ ] `xc` 不能修改最终处理结果；`wx` 可以。
- [ ] `sw` 不能执行仓库确认；`ck` 不能执行商务审核。

## 8. 全局验收清单

- [ ] 五种角色登录、退出和角色首页跳转正确。
- [ ] 新批次号不重复，并按 `WO + 日期 + 4 位流水号` 递增。
- [ ] 页面刷新后数据、照片、签字、聊天和状态不丢失。
- [ ] 所有操作日志的时间、角色、姓名和描述正确。
- [ ] 收费流程经过商务审核；零费用流程自动跳过商务。
- [ ] 三种最终结果均至少验证一次：维修完成、入库处理、无需维修退回。
- [ ] 返厂字段仅内部角色可见，现场人员看不到内部返厂信息。
- [ ] 取消批准和取消拒绝两条路径都正确。
- [ ] 桌面端和手机窄屏下均无按钮遮挡、弹窗溢出或无法滚动。
- [ ] 浏览器控制台无新的红色错误，接口无 `500`。

## 9. 缺陷记录模板

发现问题时复制以下内容：

```text
缺陷编号：BUG-
测试步骤编号：
登录角色：
批次号 / 设备序列号：
操作步骤：
预期结果：
实际结果：
页面截图：
浏览器控制台错误：
失败接口及响应：
是否稳定复现：是 / 否
```

测试完成后，不要直接删除失败批次；先保留批次号和截图，便于定位。确认缺陷记录完成后，再运行安全清理脚本：

```powershell
npx.cmd tsx scripts\clear-test-data-safe.ts
```
